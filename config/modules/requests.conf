###############################################################################
#
# Requests module configuration
#
###############################################################################

::dZSbot::Config::Set requests.storage [file join $::dZSbot::Root database requests.tsv]
::dZSbot::Config::Set requests.list_limit 10

# Legacy compatibility. If an older local config sets request.storage, the
# requests store will still honor it as a fallback.
